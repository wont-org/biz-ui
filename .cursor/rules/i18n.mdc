# i18n 国际化开发规则

## 整体架构说明

- 使用 BizProvider 作为国际化主容器，支持中文(zh)和英文(en)两种语言
- 翻译文本采用 MultiLangText 格式：`{ enUS: string, zhCN: string }`
- 通过 useTranslation hook 获取 t 函数进行翻译
- 支持参数化翻译，如：`t('common.operation.total', { total: 100 })`

## 组件国际化规则

### 1. 国际化文件组织

- **组件级翻译文件**：`src/locales/componentName.ts`
- **公共翻译文件**：`src/locales/common/[category].ts`
- **总入口文件**：`src/locales/index.ts`

### 2. 组件国际化实施步骤

#### 步骤 1：检查公共翻译

在创建组件翻译前，必须检查`src/locales/common/`目录：

- 如果需要的翻译已存在，直接使用公共翻译
- 如果是通用术语（如操作按钮、表单字段），应添加到对应的公共文件：
  - `operation.ts`：操作类翻译（确定、取消、删除、编辑、添加、更新等）
  - `form.ts`：表单类翻译（请输入、请选择、必填等）
  - `message.ts`：操作结果翻译（成功/失败消息、确认提示等）
  - `status.ts`：状态类翻译（加载中、无数据、总计、已选择等）
  - `ui.ts`：界面类翻译（设置、帮助等）
  - 其他类别根据需要创建新文件

**公共翻译提取原则**：

- **操作类**：所有 CRUD 操作按钮（增删改查、保存、提交等）
- **表单类**：所有通用输入提示、验证消息、占位符（如"请选择"、"请输入"、"必填项"）
- **消息类**：所有成功/失败提示、确认对话框
- **状态类**：加载状态、数据状态、统计信息等
- **界面类**：设置、帮助等通用界面元素
- **重复文案检查**：相同文案在 2 个以上组件中使用，且为通用文案时才提取

**严格不提取的内容**：

- **组件特有业务文案**：如"请选择颜色"、"请选择图标"等与组件业务逻辑强相关的文案
- **业务术语**：如 ConditionColor、ConditionIcon 等组件的专业术语
- **组件常量**：组件常量定义中的 labelKey 保持组件内部翻译
- **特定场景文案**：只在特定业务场景使用的翻译
- **带业务限定词的文案**：凡是包含具体业务对象的选择/输入提示

#### 步骤 1.5：公共文案检查流程

在创建新翻译前，必须检查是否存在重复文案：

1. **搜索相同文案**：在整个项目中搜索相同或相似的中英文文案
2. **判断通用性**：如果文案在 2 个以上组件中使用，评估是否为通用文案
   - **通用文案标准**：不包含具体业务对象、可在任意表单中使用
   - **业务文案标准**：包含具体业务对象、与组件功能强相关
3. **提取公共翻译**：
   - 通用表单提示（如"请选择"、"请输入"、"必填项"）→ `common.form`
   - 通用操作结果（如"提交成功"、"验证失败"）→ `common.message`
   - 通用操作按钮（如"验证"、"重试"）→ `common.operation`
4. **更新使用位置**：将组件中的重复翻译改为引用公共翻译

**示例检查过程**：

```bash
# 搜索重复文案
grep -r "请选择" src/
grep -r "Please select" src/

# 判断是否为通用文案
# ✅ "请选择" → 通用提示，提取到 common.form.select
# ❌ "请选择颜色" → 组件特有，保持在 conditionColor.ui.selectColorRequired
# ❌ "请选择图标" → 组件特有，保持在 conditionIcon.ui.iconRequired

# 只有通用的"请选择"可以提取
# 更新组件使用：t('common.form.select')
```

#### 步骤 2：创建组件翻译文件

文件路径：`src/locales/componentName.ts`

```typescript
export const componentName = {
  title: {
    enUS: 'Component Title',
    zhCN: '组件标题',
  },
  description: {
    enUS: 'Component description',
    zhCN: '组件描述',
  },
  // ... 其他翻译项
};
```

#### 步骤 3：注册翻译文件

在`src/locales/index.ts`中导入并注册：

```typescript
import { componentName } from './componentName';

export const LOCALE_DATA = {
  common: {
    operation,
    form,
  },
  componentName, // 添加新组件
  // ... 其他组件
};
```

### 3. 组件中使用国际化

#### 在 React 组件中使用翻译：

```typescript
import { useTranslation } from '@wont/biz-ui/BizProvider';

const Component = () => {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t('componentName.title')}</h1>
      <p>{t('common.operation.confirm')}</p>
      <span>{t('common.operation.total', { total: 100 })}</span>
    </div>
  );
};
```

#### 在常量定义中使用翻译：

**标准处理方案（labelKey 方式）**：

常量中的翻译文本必须使用 `labelKey` 方式，优先使用公共翻译：

```typescript
// ✅ 推荐：使用公共翻译的 labelKey
export const OPERATE_TYPE = {
  add: {
    labelKey: 'common.operation.add', // 使用公共翻译
    value: 'add' as const,
  },
  edit: {
    labelKey: 'common.operation.edit', // 使用公共翻译
    value: 'edit' as const,
  },
  delete: {
    labelKey: 'common.operation.delete', // 使用公共翻译
    value: 'delete' as const,
  },
};

// ✅ 推荐：组件特定的常量使用组件翻译
export const CONFIG_OPTIONS = [
  {
    value: 'option1',
    labelKey: 'componentName.options.option1',
  },
  {
    value: 'option2',
    labelKey: 'componentName.options.option2',
  },
];

// ✅ 在组件中使用
const Component = () => {
  const { t } = useTranslation();

  const localizedOptions = CONFIG_OPTIONS.map((option) => ({
    ...option,
    label: option.labelKey ? t(option.labelKey) : option.label,
  }));

  return <Select options={localizedOptions} />;
};
```

**其他可选方案**：

- 静态多语言对象：`{ label: { enUS: 'Text', zhCN: '文本' } }`
- 移动到组件内部处理

**遇到新的未覆盖情况时再咨询开发者**。

**注意**：不可在常量定义中直接调用翻译方法或 Hook。

### 4. 代码修改限制

#### 核心原则：

- **禁止修改业务逻辑**：只能添加翻译调用，不能改变组件的功能逻辑
- **只能添加翻译**：将硬编码文本替换为翻译调用
- **保持组件 API 不变**：不能修改 props 接口和导出方式

#### 常量翻译处理：

**标准做法**：使用 labelKey 方式处理常量中的翻译文本。遇到新的未覆盖情况时再咨询开发者。

### 5. 公共翻译使用规范

#### 已有公共翻译类别

**操作类翻译 (`common.operation.*`)**：

- 基础操作：add(添加)、edit(编辑)、delete(删除)、view(查看)、update(更新)
- 确认操作：confirm(确定,Ok)、cancel(取消)
- 表单操作：save(保存)、submit(提交)、reset(重置)
- 其他操作：search(搜索)、refresh(刷新)、copy(复制)、upload(上传)、retry(重试)等

**表单类翻译 (`common.form.*`)**：

- 输入提示：input(请输入)、select(请选择)、date(请选择日期)
- 验证消息：required(该字段是必填字段)
- 范围字段：min(最小值)、max(最大值)、startDate(开始日期)、endDate(结束日期)

**消息类翻译 (`common.message.*`)**：

- 成功消息：success.add(添加成功)、success.edit(编辑成功)、success.delete(删除成功)等
- 失败消息：error.add(添加失败)、error.submit(提交失败，请检查表单)等
- 确认对话框：confirm.delete.title(确认删除？)、confirm.delete.content(确认删除此项吗？)

**状态类翻译 (`common.status.*`)**：

- 加载状态：loading(加载中...)
- 数据状态：noData(暂无数据)、more(更多)、collapse(收起)、expand(展开)
- 统计信息：total(共 {total} 项)、selected(已选择 {count} 项)

**界面类翻译 (`common.ui.*`)**：

- 通用界面：settings(设置)、help(帮助)

#### 公共翻译使用流程

1. **首先检查公共翻译**：在为组件添加新翻译前，必须先检查是否已有对应的公共翻译
2. **优先使用公共翻译**：如果公共翻译已存在，必须使用公共翻译，不得重复创建
3. **评估是否需要新增公共翻译**：如果该术语具有通用性，应添加到公共翻译文件中
4. **组件特定翻译**：只有组件特有的业务术语才放在组件翻译文件中

#### 常量中使用公共翻译

```typescript
// ✅ 正确：常量定义中使用公共翻译
export const OPERATE_TYPE = {
  add: {
    labelKey: 'common.operation.add',
    value: 'add' as const,
  },
  edit: {
    labelKey: 'common.operation.edit',
    value: 'edit' as const,
  },
};

// 在组件中动态翻译
const options = Object.values(OPERATE_TYPE).map((option) => ({
  ...option,
  label: t(option.labelKey),
}));
```

### 6. Demo 文件规范

#### demo/basic.tsx 文件要求：

- **按组件分类**：根据 src 目录结构组织 demo 展示
- **精简代码**：移除冗余代码，只保留核心演示功能
- **统一布局**：使用 Card + Divider 进行区块划分
- **语言切换**：提供中英文切换功能
- **国际化演示**：每个组件都要展示国际化效果

#### 组件文档国际化要求：

- **创建英文文档**：如果存在 `index.md` 中文文档，必须创建对应的 `index.en-US.md` 英文文档
- **文档分组翻译标准化**：文档中的分组名称必须按照统一标准翻译
  - `数据录入` → `Data Collect`
  - `数据展示` → `Data Display`
  - `经典案例` → `Classic Cases`
  - `通用` → `General`
  - `Dom` → `Dom`
  - `国际化` → `i18n`
- **Demo 文件国际化**：demo 文件中的所有硬编码文本都必须国际化处理
- **Demo 翻译文件隔离**：demo 专用的翻译文件必须独立存放，不能写入组件源码的翻译文件中
- **文档结构对照**：
  ```
  ComponentName/
  ├── index.md                    # 中文文档
  ├── index.en-US.md              # 英文文档
  └── demo/                       # 演示代码（已国际化）
      ├── BasicDemo.tsx           # 使用BizUIProvider包装，支持语言切换
      ├── AnotherDemo.tsx
      ├── locales/                # demo专用翻译文件目录
      │   ├── basicDemoLang.ts    # 基础demo翻译文件
      │   ├── anotherDemoLang.ts  # 另一个demo的翻译文件
      │   └── ...
      └── ...
  ```
- **Demo 国际化要求**：

  - 所有硬编码中文文本使用 `t()` 函数翻译
  - **Demo 组件不需要语言切换组件**，跟随 dumi 的多语言配置
  - **使用 dumi 的 useLocale 获取当前语言**：`import { useLocale } from 'dumi'`
  - dumi 语言配置：`zh-CN` / `en-US`（与 BizUIProvider 的 Language 类型一致）
  - 颜色标签、按钮文字、说明文字等都要国际化
  - 保持 demo 功能和交互逻辑不变
  - **Demo 翻译文件命名规范**：`[component]/demo/locales/[demoFileName]Lang.ts`
  - **Demo 翻译文件使用**：通过 BizUIProvider 的 localeData 属性传入，不污染组件源码翻译
  - **导入路径标准化**：国际化相关的导入必须使用 `@wont/biz-ui` 别名：

    ```typescript
    // ✅ 正确：使用 dumi 的多语言支持
    import { useLocale } from 'dumi';
    import { BizUIProvider } from '@wont/biz-ui';
    import { useTranslation } from '@wont/biz-ui/BizProvider/index';
    import { getAntdLocale } from '@wont/biz-ui/BizProvider/hooks';
    import ComponentName from '@wont/biz-ui/ComponentName';

    // ✅ Demo 翻译文件使用示例
    import { demoLangData } from './locales/demoNameLang';

    const DemoComponent = () => {
      const { id: locale } = useLocale(); // dumi 提供的语言获取
      return (
        <BizUIProvider locale={locale} localeData={demoLangData}>
          {/* demo 内容，不需要语言切换组件 */}
        </BizUIProvider>
      );
    };

    // ❌ 错误：在 demo 中使用自定义语言切换
    const [locale, setLocale] = useState<Language>('zh-CN');
    <LanguageSwitcher locale={locale} setLocale={setLocale} />;
    ```

- **翻译内容包括**：
  - 组件标题和描述
  - 功能说明文字
  - 示例说明
  - Demo 中的所有中文文本
  - API 文档中的描述文字

#### demo 组织结构：

```typescript
import { useLocale } from 'dumi';
import { BizUIProvider } from '@wont/biz-ui';
import { getAntdLocale } from '@wont/biz-ui/BizProvider/hooks';
import ComponentName from '@wont/biz-ui/ComponentName';
import { demoLangData } from './locales/demoNameLang';

export default () => {
  const { id: locale } = useLocale(); // 使用 dumi 的语言获取

  return (
    <BizUIProvider locale={locale} localeData={demoLangData}>
      <ConfigProvider locale={getAntdLocale(locale)}>
        {/* 直接展示组件演示，不需要语言切换器 */}
        <ComponentDemo />
      </ConfigProvider>
    </BizUIProvider>
  );
};
```

## 开发检查清单

### 国际化实施前：

- [ ] 检查 common 目录下是否有可复用的翻译
- [ ] 确认组件中所有硬编码文本
- [ ] 识别常量定义中的文本内容（使用 labelKey 方式处理）
- [ ] 设计翻译 key 的命名规范

### 国际化实施中：

- [ ] 创建或更新组件翻译文件
- [ ] 在 index.ts 中注册翻译文件
- [ ] 替换组件中的硬编码文本为 t()调用
- [ ] 处理常量中的文本（使用 labelKey 方式）
- [ ] 测试中英文切换效果

### 国际化实施后：

- [ ] 确认没有修改业务逻辑
- [ ] 确认常量处理使用了 labelKey 方式
- [ ] 确认翻译 key 命名规范
- [ ] 在 demo 文件中添加组件演示
- [ ] 创建英文文档 index.en-US.md
- [ ] Demo 文件国际化处理
- [ ] 测试完整的国际化流程

## 常见翻译 key 命名规范

### 组件级翻译：

- `title` - 组件标题
- `description` - 组件描述
- `placeholder` - 占位符文本
- `tooltip` - 提示文本
- `error` - 错误信息
- `success` - 成功信息
- `warning` - 警告信息

### 公共翻译：

- `common.operation.*` - 操作类（确定、取消、删除等）
- `common.form.*` - 表单类（请输入、请选择等）
- `common.status.*` - 状态类（加载中、成功、失败等）
- `common.time.*` - 时间类（开始时间、结束时间等）

### 常量翻译：

- `colors.*` - 颜色相关
- `sizes.*` - 尺寸相关
- `types.*` - 类型相关
- `options.*` - 选项相关

## 注意事项

1. **优先使用公共翻译**：避免重复定义相同含义的翻译
2. **保持 key 语义清晰**：使用有意义的 key 名称，便于维护
3. **支持参数化**：对于包含变量的文本，使用`{variableName}`格式
4. **常量处理标准化**：常量翻译使用 labelKey 方式，遇到新的未覆盖情况时再咨询开发者
5. **测试完整性**：确保所有文本都能正确切换语言
6. **保持一致性**：同类功能使用相同的翻译 key 模式
7. **避免运行时错误**：不在模块顶层或常量定义中调用翻译方法或 Hook
