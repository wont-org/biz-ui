import { Popover } from 'antd';
import { styled } from 'styled-components';

export const StylePopoverReset = styled(Popover)`
  .color-picker-popover {
    .ant-popover-inner-content {
      padding: 12px 8px;
    }
  }
`;
