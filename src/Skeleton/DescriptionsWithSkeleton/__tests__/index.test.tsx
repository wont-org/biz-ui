import React from 'react';
import { render, screen } from '@testing-library/react';
import DescriptionsWithSkeleton from '..';

describe('DescriptionsWithSkeleton', () => {
  it('应该正确渲染组件', () => {
    render(<DescriptionsWithSkeleton />);
    // 这里添加断言
  });

  // 添加更多测试用例
});
