import React from 'react';
import { render, screen } from '@testing-library/react';
import MultiExpand from '..';

describe('MultiExpand', () => {
  it('应该正确渲染组件', () => {
    render(<MultiExpand />);
    // 这里添加断言
  });

  // 添加更多测试用例
});
