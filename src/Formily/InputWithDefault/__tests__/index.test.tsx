import React from 'react';
import { render, screen } from '@testing-library/react';
import InputWithDefault from '..';

describe('InputWithDefault', () => {
  it('应该正确渲染组件', () => {
    render(<InputWithDefault />);
    // 这里添加断言
  });

  // 添加更多测试用例
});
