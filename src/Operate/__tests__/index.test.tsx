import React from 'react';
import { render, screen } from '@testing-library/react';
import Operate from '..';

describe('Operate', () => {
  it('应该正确渲染组件', () => {
    render(<Operate />);
    // 这里添加断言
  });

  // 添加更多测试用例
});
