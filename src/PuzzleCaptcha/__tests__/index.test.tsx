import React from 'react';
import { render, screen } from '@testing-library/react';
import PuzzleCaptcha from '..';

describe('PuzzleCaptcha', () => {
  it('应该正确渲染组件', () => {
    render(<PuzzleCaptcha />);
    // 这里添加断言
  });

  // 添加更多测试用例
});
